---
// Blog index — /blog
// Required features per CLAUDE.md §5.4:
//   - Pulls from WordPress REST API (src/lib/wordpress.ts)
//   - Pagination
//   - Category filter
//   - Estimated read time + last-updated date per post
//
// Data: getRecentPosts() / getAllPosts() from src/lib/wordpress.ts
// Note: build-time fetch preferred for SEO + speed (see CLAUDE.md §4)

import BaseLayout from '../../layouts/BaseLayout.astro'

// TODO: uncomment when lib is implemented
// import { getAllPosts } from '../../lib/wordpress'
// const posts = await getAllPosts()
---

<BaseLayout
  title="Blog"
  description="[DRAFT] Practical guides on building supplemental income online. Affiliate marketing, digital products, and content monetization — no fluff."
>
  <div class="container-wide section-padding">
    <h1 class="text-3xl font-bold mb-4">Blog</h1>
    <p class="text-gray-600 mb-12">
      [DRAFT] Real strategies for building income online. No hype — just what actually works, how
      long it takes, and what it costs.
    </p>

    <!-- TODO: category filter tabs -->
    <!-- TODO: post grid — pulls from WordPress REST API -->
    <!-- TODO: pagination controls -->

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
      <p class="text-gray-400 col-span-full text-center py-12">
        [Blog posts will load from WordPress — lib/wordpress.ts to be implemented]
      </p>
    </div>
  </div>
</BaseLayout>
